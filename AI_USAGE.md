ในการทำงานนี้มีการใช้ AI เพื่อช่วยอธิบายโครงสร้างของงาน Agentic RAG
ช่วยเขียนและตรวจสอบโค้ดในแต่ละขั้นตอน รวมถึงช่วยวิเคราะห์ข้อผิดพลาดจากการรันจริง โดยใช้
AI ช่วยในส่วน Chunk + Embedding + Qdrant, Agent + Custom Tool และ RAG
Agent + LLM-as-Judge นอกจากนี้ยังใช้ AI ช่วยวิเคราะห์ Traceback เมื่อ Step 2
ไม่สามารถทำงานได้ ในตอนแรกได้รับคำแนะนำให้ตรวจสอบ API Key และการตั้งค่า Colab
Secrets แต่จากผลการรันจริงพบว่า API Key สามารถใช้งานได้แล้ว และปัญหาที่แท้จริงคือ
429 RESOURCE_EXHAUSTED จาก quota ของโมเดล gemini-3.1-pro ซึ่งมี quota
เป็น limit: 0 สำหรับ Free Tier จึงแก้ไขโดยเปลี่ยนโมเดลที่ใช้ใน Agent เป็น
gemini-3.6-flash และปรับการเรียกใช้งานให้ตรงกับโมเดลที่สามารถใช้งานได้กับ API
Key ของผู้จัดทำ หลังจากแก้ไขแล้ว Agent และขั้นตอนที่เกี่ยวข้องสามารถรันได้สำเร็จ

การใช้ AI

AI ถูกใช้เพื่อ: - ช่วยอธิบายแนวทางการทำ Chunking, Embedding และการจัดเก็บข้อมูลใน
Qdrant - ช่วยเขียนและปรับโค้ด Custom Tool สำหรับคำนวณพื้นที่สี่เหลี่ยมผืนผ้า -
ช่วยสร้างและตรวจสอบโครงสร้าง Google ADK Agent - ช่วยสร้าง RAG Tool ที่ค้นข้อมูลจาก
Qdrant - ช่วยวิเคราะห์และแก้ไข Error จากการรันจริง - ช่วยตรวจสอบการทำงานของ
LLM-as-Judge

Prompt ที่ใช้

ตัวอย่าง prompt ที่ใช้ ได้แก่ การขอให้ช่วยเขียนโค้ดสำหรับแต่ละขั้นตอนของ Agentic RAG,
ขอให้ช่วยตรวจสอบว่าโค้ดสามารถทำงานตามโจทย์ได้หรือไม่ และขอให้ช่วยวิเคราะห์ Traceback
เมื่อ Agent เรียก Gemini API แล้วเกิดข้อผิดพลาด 429 RESOURCE_EXHAUSTED

สิ่งที่แก้ไขจากคำแนะนำของ AI

หลังจากได้รับคำแนะนำ ได้มีการปรับโค้ดให้เหมาะกับสภาพแวดล้อมที่ใช้งานจริง
โดยเฉพาะส่วนของโมเดล Gemini จากเดิมที่ใช้ gemini-3.1-pro-preview
ซึ่งไม่สามารถใช้งานได้เนื่องจาก quota เป็นศูนย์ จึงเปลี่ยนเป็น gemini-3.6-flash
และทดสอบใหม่จนสามารถรันได้สำเร็จ

จุดที่ AI แนะนำไม่ตรงกับเคสจริง

ในช่วงแรก AI แนะนำให้ตรวจสอบ API Key และการตั้งค่า Secrets
เนื่องจากยังไม่ทราบสาเหตุของ Error อย่างไรก็ตาม เมื่อดู Traceback จริงพบว่า API Key
ถูกส่งไปยัง Gemini แล้ว แต่ API ตอบกลับด้วย 429 RESOURCE_EXHAUSTED และระบุว่า
quota ของ gemini-3.1-pro มีค่า limit: 0 ดังนั้นปัญหาจริงไม่ได้เกิดจาก API Key
แต่เกิดจาก quota ของโมเดลที่เลือกใช้ จึงต้องเปลี่ยนโมเดลแทนการสร้างหรือเปลี่ยน API Key

สรุป

การใช้ AI ช่วยลดเวลาในการเขียนโค้ดและวิเคราะห์ปัญหา
แต่ต้องตรวจสอบผลลัพธ์จากการรันจริงด้วยตนเอง โดยเฉพาะ Error จาก API
ซึ่งคำแนะนำเบื้องต้นอาจไม่ตรงกับสาเหตุจริง การอ่าน Traceback และตรวจสอบข้อความ
Error ทำให้สามารถระบุสาเหตุของปัญหาและเลือกวิธีแก้ไขที่เหมาะสมได้
