Query ที่พังเป็นคำถาม “ห้องสี่เหลี่ยมกว้าง 5 เมตร และยาว 8 เมตร มีพื้นที่เท่าไร?” โดยเกิดข้อผิดพลาดระดับ **กลาง (API/Quota Error)** เนื่องจาก Agent 
พยายามเรียกใช้โมเดล `gemini-3.1-pro` แต่ Free Tier ของ API ไม่มี quota สำหรับโมเดลนี้ ทำให้เกิด `429 RESOURCE_EXHAUSTED` และระบุ `limit: 0` 
ก่อนแก้ไข Agent ถูกตั้งให้ใช้ `gemini-3.1-pro-preview` ซึ่งทำให้การเรียก Gemini ล้มเหลว หลังจากตรวจสอบ Trace จึงเปลี่ยนโมเดลเป็น `gemini-2.5-flash`
เพื่อให้เหมาะกับ quota ของ API ที่ใช้งาน และสามารถทดสอบ Agent กับ Custom Tool ต่อได้ โดยปัญหานี้ไม่ได้เกิดจาก logic ของ Custom Tool แต่เกิดจากข้อจำกัดของ API quota.
